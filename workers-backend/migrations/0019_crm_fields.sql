-- CRM fields for accounts (no-op: columns already exist in production)
-- phone, tags, contacts, interactions, website, address, company_size,
-- contract_start, contract_end, contract_value, logo_url, nps, last_contact_date
-- were added manually before this migration was tracked.
SELECT 1;
